@using DMS.Models
@model Defect

@using (Html.BeginForm("Fix", "Defect", new { @id = Model.Row_id }, FormMethod.Post))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="col-sm-3 pull-right col-sm-offset-1">
        <button type="submit" class="btn btn-success form-control"> Fix </button>
    </div>
}
